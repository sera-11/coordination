<p style="color: green"><%= notice %></p>

<div class="container">

  <%= render @organization %>

  <footer class="text-center">
    <%= link_to "Back to organizations", organizations_path %> | 
    <%= link_to "Edit #{@organization.name}", edit_organization_path(@organization) %> |
    <%= link_to "Delete #{@organization.name}", @organization, method: :delete %>
  </footer>

</div>
